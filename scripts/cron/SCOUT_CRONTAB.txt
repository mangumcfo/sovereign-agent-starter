# /goal Scout — DETERMINISTIC overnight cadence (ratified GB_goal_Scout_Synthesis_2026-06-15.md; KM [374/375]).
# Pilot-gated (GB [375] #1): installed only after the 3-title pilot proved zero-slop. KM [378] approved go-live.
#
# Install: crontab -l > /tmp/ct && cat scripts/cron/SCOUT_CRONTAB.txt >> /tmp/ct && crontab /tmp/ct
#
# What it runs: deterministic derivers ONLY — no LLM agent, ~zero tokens, propose-only (writes packets under
# artifacts/scout/ and posts candidate proposals KM+GB disposition; never creates a real obligation, commits,
# or seals). The expensive/judgment lanes stay Atrium-gated (one human gate), exactly like the crypto cadence.
# REQUIRES: the python3 on PATH has the analysis deps (pyyaml + ruff/vulture) — installed into ~/.local so the
# static-scan delta is real, not silently empty. cd into the repo first (cron runs from $HOME).
#
# Nightly 03:10: refresh Book↔Code drift → refresh static-scan delta → derive+lint+propose the packets.
10 3 * * * cd /home/kmangum/work-repos/sovereign-agent-starter && python3 scripts/build_book_code_tree.py >/dev/null 2>&1; python3 scripts/scout_run.py >> artifacts/scout/cron-$(date +\%Y\%m\%d).log 2>&1
