# S2 Completion & Surface-Harden Rail (GB spec — Tiger executes)
*KM directive 2026-06-14: complete S2 FIRST. Validate all S2 manuscripts against the UPDATED workflow + alignment with the just-published V1; get every title fully vetted + extruded, all aspects complete; harden the S2 surface; then quickly turn to S3. Kick Tiger off.*

## Baseline (ground-truth, verified from vault — not roadmap text)
- **V1 Sovereign Inference & Memory = PUBLISHED.** It is the concrete "done" template (official KDP description, _round1/2/3 boards, km_sitting verify, DISPATCH_MANIFEST, KDP_SUBMISSION_CONFIRMATION, full cover set, deterministic render). **Match V2–V5 to this bar.**
- **V2–V5 are vetted but predate the bar:** manuscripts at v1.3, 3 board rounds (v1.0/1.1/1.2), full metadata, complete pb+hc cover sets, rendered PDFs, handoff packets — all present. Handed off **2026-05-30**, before the Review-Ready Rail amendments, the Tech/Arch board, the cover-set convention, the bl-verify-backed description, and a materially-changed engine (universalize wave etc.).
- **This is delta-closing + re-validation, NOT re-building.**

## CONFIRMED gaps (from inventory)
1. **Official KDP book description MISSING for V2–V5** (only V1 has `KDP_BOOK_DESCRIPTION_OFFICIAL.md`).
2. **Updated Review-Ready Rail not run on V2–V5** — boards are the pre-amendment 3-round set; missing Cold Reader (7th persona), R1.5 board rigor + R1.5g sampled adversarial audit, fidelity pass, Fold Report + Brief.
3. **Alignment vs the NOW-current platform un-rechecked** — books aligned to a 2026-05-30 platform; the engine has changed since.
4. **Tech/Arch board** (KM-mandated, runs right after UX board) — no evidence it ran on V2–V5.
5. **Deterministic-Render / Gate 6 renderability** — V2–V5 PDFs predate the standard; re-verify.
6. **Cover-set series consistency** — wraps exist but predate the series-level cover-set convention + the V1 published cover; verify consistency, regenerate only if inconsistent.
7. **Controlled-Link doctrine / bl-verify** — align each book's verify-promise with V1's (bl-verify clean on public sealed repo).

## The per-volume rail (V2, V3, V4, V5 — each to V1-published standard)
For each volume, in order:
1. **Validate against the updated workflow** — produce a gap sheet (this volume vs the current WORKFLOW + V1 bar). *This is step 1 KM asked for; surface it before deep work.*
2. **Re-run the amended Review-Ready Rail deltas** — Cold Reader + R1.5 board rigor + R1.5g sampled adversarial audit + fidelity pass; **Tech/Arch board right after the UX board (no defer)**.
3. **Alignment lenses vs current platform** — book↔platform (do operational claims still map to live specs/code after the engine changes?), book↔UX (Atrium's actual current state — *critical for V2 Primacy Cockpit = Atrium and V3 governed dev loop = current proposal/obligation mechanics*), book↔book canonical-term (peer-role etc.), author-imposition audit.
4. **Spec extraction / living-spec mapping** — operational claims → specs (the `living_spec_mapping_to_role_lattice` packets), co-extruded.
5. **Official KDP book description** — author `KDP_BOOK_DESCRIPTION_OFFICIAL.md` to V1's voice/format; controlled-link + bl-verify promise where claimed.
6. **Cover-set consistency check** — vs series cover-set convention + V1 cover; regenerate only on inconsistency.
7. **Deterministic re-render (Gate 6)** — rebuild the PDF clean under the standard.
8. **Fold Report + Brief** — per volume, the rail's exit artifact.
9. **State → `awaiting_human_review`** at the full V1-published standard (all aspects complete).

## Surface harden (after all 4 pass)
Series-level coherence + alignment sweep: cross-volume canonical-term consistency, the operator→architect→family-CIO→steward arc intact, controlled-link/bl-verify promise consistent across all 5, cover set visually consistent as a series, descriptions consistent. Output: a **series-level readiness digest for KM** (per-volume Fold Report + the gap-closure evidence).

## Cadence
- Per current_arc "one volume at a time," but V2–V5 are independent — Tiger may pipeline. The rail is per-volume; gaps surface as cards.
- **GB checks the validation gap-sheets FIRST** (the moment-1 output), then checks complete + runs an alignment/fidelity audit at the end (the GB-checks-plan / Tiger-executes-uninterrupted / GB-checks-complete pattern).
- Then: **quickly turn to S3** (GB re-verifies S3 V4 actual file state at that pivot — the roadmap S3 text is suspect/stale).

## Engine-track note
95+ remains the standing engine priority (the 4-HIGH finish-pass incl. the O(n²) Merkle fix). S2 completion is the immediate book-track priority KM kicked off; engine hardening is the lane Tiger returns to after the S2 surface is hardened (mirrors "harden S2, then turn to S3"). If KM wants them interleaved, he'll say.

∞Δ∞ SEAL: spec — S2 is vetted, not blank. Re-validate V2–V5 to the V1-published bar against the current workflow, close the confirmed deltas (official descriptions · amended rail + Tech/Arch board · platform re-alignment · deterministic render · cover/verify consistency), harden the series surface, hand KM a clean readiness digest. Then S3.
