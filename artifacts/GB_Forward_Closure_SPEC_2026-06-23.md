# Forward-Closure Execution SPEC (GB → Tiger, 2026-06-23)
*KM confirmed the closing targets. This is the precise edit spec for 100% close-out — zero open/vague forwards before any S3 volume publishes. Tiger executes the manuscript edits; GB specified; the `forward_reference` gate verifies after. Principle for every edit: replace vague "design direction / the broader series / a later volume" with the NAMED closing series below, in present receipt language (RUNS-TODAY vs DESIGNED-TOWARD, closes in S<n>).*

## KM-confirmed closing homes (the substitution key)
- **External anchor / outsider-verification / cross-org trust → S6 Inter-Node Sovereignty + S7 Zero-Trust Sovereignty** (name both; the published-root + split-key custody is the zero-trust primitive).
- **Vertical products · autonomous self-building · governance-engine depth (per-verb DSL / SOD auto-refusal / entity-scope) · performance-migration · cockpit convenience layer → S5 Full Production ERP** (S3 explicitly feeds S5's 32-title expansion).
- **V3 dangling self-healing reference → re-home to S5 Full Production ERP** (it currently names a non-existent "later S3 volume").
- **Subsidized continuity paths → code-forward, closes in S6/S7** (name it like the other code forwards, not a vague philosophy).

## Edit list — by volume (receipt-level; keep S2's honest RUNS-TODAY/DESIGNED-TOWARD voice)
**S3 V1 (immutable_core):**
- Ch8 receipt (~L980-981) + Ch8 "See It Work" step 5 (~L1009-1010): external anchor / residual-trust-root → append "closes in **S6 Inter-Node / S7 Zero-Trust**."
- Ch7 (~L266-276) migration story → "the migration is **designed-toward; closes in S5 Full Production**."
- Ch6 (~L248-250) cockpit convenience layer → "the convenience lens is **designed-toward; closes in S5 Full Production**."

**S3 V2 (governance_skin):**
- Ch1 receipt (~L208-209) full declarative YAML engine → "**closes in S5 Full Production**."
- Ch2 receipt (~L315-316) entity-scope enforcement → "**closes in S5**."
- Ch3 receipt (~L449-453) per-verb condition engine / SOD auto-refusal / role-matrix → "**closes in S5**."
- Ch5 (~L611-620) industry pattern library → "the library is accreted across **S5 Full Production** (the 32-title vertical expansion)."
- Ch6 receipt (~L777-778) autonomous self-building → "**closes in S5 Full Production**."

**S3 V3 (helix) — INCLUDES THE CRITICAL DANGLING FIX:**
- Ch7 (~L946-951) richer self-healing — **CHANGE the closing target**: currently "this series' later volume on drift, self-healing, generational config (Series 3)" → must become "**closes in S5 Full Production** (built on the detection-and-rollback primitives shipped here)." (V4 is the last S3 volume; the old reference dangles.)
- Ch8 receipt (~L976-981) external anchor → "closes in **S6/S7**."
- Ch6 (~L726-735) autonomous self-building → "**closes in S5 Full Production**."

**S3 V4 (industry_sovereign_erps) — capstone, carries the master table:**
- Ch1 receipt (~L166-173) + Ch2-5 each vertical-product receipt → "the per-industry products are **designed-toward; close in S5 Full Production** (the 32-title expansion this volume's patterns seed)."
- Ch4 (~L372-376) cross-org trust → tighten "broader series" to explicit "**S6 Inter-Node / S7 Zero-Trust**."
- Ch7 (~L617-625) external anchor / outsider-verifiable handoff → "**closes in S6/S7**."
- Ch7 (~L604-606) subsidized continuity paths → reframe as a **code-forward**: "a buildable continuity capability that **closes in S6/S7** (federation/inheritance)," not just "a design direction."
- **Ch8 — ADD the consolidated FORWARD-CLOSURE TABLE (below).**

**S2 loose ends (4 spec pointers — add named closures):**
- V1 Preflight: B51 attention-stream spec `(planned)` → `(planned — lands in Vol 3 Ch 4)`.
- V1 Ch7: red-team cadence spec `(planned)` → `(planned — lands in Vol 3)`.
- V2 (all 8+ instances): atrium operator role spec `(planned)` → `(planned — lands in Vol 3, role-spec deep-dive)`.
- V2 Ch3: proposal mechanics spec `(planned)` → `(planned — lands in Vol 3 §2a)`.
*(These are companion-repo spec pointers; the Authoritative Pattern Rule deferral is fine — just name the home.)*

## The V4 Ch8 consolidated FORWARD-CLOSURE TABLE (insert verbatim, Tiger may format to house style)
> **Where the road continues.** This volume completes Series 3 — the sovereign-ERP foundation. What it builds runs today; what it points toward is named here so nothing is left as vague "someday." Each forward below closes in a specific later series, by design.
>
> | Forward capability | Runs today (the foundation) | Closes in |
> |---|---|---|
> | Per-industry vertical *products* (dairy, manufacturing, electronics, services, family-office) | the sealed/replayable substrate + the declared patterns | **S5 Full Production ERP** (the 32-title vertical expansion) |
> | Autonomous self-building ERP (system extends itself under the gate) | governed extensions as gated, sealed, human-approved proposals | **S5 Full Production ERP** |
> | Full governance engine (per-verb condition DSL, SOD auto-refusal, entity-scope, role-matrix) | the core gates + evidences material actions; records originator + approver | **S5 Full Production ERP** |
> | Richer self-healing render (re-derive a corrected render, propose it) | drift scoring + rollback-to-last-faithful | **S5 Full Production ERP** |
> | Performance / migration at million-entry scale | the small, auditable immutable core | **S5 Full Production ERP** |
> | External anchor — outsider-provable proof without trusting the keyholder | self-verifying export packet + clean-machine replay | **S6 Inter-Node + S7 Zero-Trust** |
> | Cross-organization trust / federation | single-node provenance + the export packet | **S6 Inter-Node + S7 Zero-Trust** |
> | Subsidized continuity paths (low-cost verified handoff to heirs/employees/community) | the deterministic, self-verifying export package | **S6/S7** (federation + inheritance) |
>
> The foundation is sealed; the road is named. — Series 3 close

## Verification (Tiger, after edits)
1. Re-run `review_ready_contract` on V1/V2/V3/V4 → `forward_reference` gate stays GREEN (every forward now names a closing series).
2. Grep each manuscript for residual vague forwards: `designed-toward` / `broader series` / `later volume` with NO adjacent `S[4-9]` / `Series [4-9]` → should be zero.
3. Confirm the V3 self-healing reference no longer names a non-existent S3 volume.
4. Archive this closure pass's board findings into the permanent ledger.
5. GB spot-verifies the V4 Ch8 table + the V3 dangling fix on re-surface.

∞Δ∞ Every forward now names its home: full-production in S5, outsider-proof in S6/S7. No loose ends ship. — GB
