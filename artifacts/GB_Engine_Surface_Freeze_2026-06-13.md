# Engine Surface Freeze — KM-ratified 2026-06-13 (standing until 95+ confirmed)
*Purpose: stop auditing a moving target. Harden the engine to 95+ earned-clean (GB_Audit_to_95_Plan), then un-freeze and scale on a clean base. The test asks one question: does it ADD net-new engine surface, or REDUCE findings / move books?*

## FROZEN — no net-new engine/platform surface
- New API endpoints, new modules, new capabilities on the sovereign-agent stack.
- The queued net-new builds: **S9 Vol-3 "Human Cockpit" engine build · migration-arc (s5_33–37) code · any new R-22-style capability · new seeit engine features.** (Their BOOK side continues — see unfrozen.)
- Rule: if it makes the engine *do something new*, it waits until 95+ confirmed.

## ALLOWED — cleanup / hardening (this IS the work; it reduces findings)
- All audit-finding fixes: Tiger [814] Phases A→D (constitutional gate, trust boundary, visibility, packaging).
- **The Open Card Parity harness** — net-new code but explicitly a *finding-class closer* (the audit validated it); it's remediation, not feature. Proceeds as W1's visibility close.
- Test-coverage waves (W2), refactors/dedup/complexity reduction, packaging/lockfile (W3).
- Durability commits. The render-standard lint / figure-legibility checks (quality gates).

## UNFROZEN — books & content (do not touch the engine)
- Wave-1 manuscript work (Vol 1 figures/links/ship), all manuscript/editorial/board work.
- S9 outline + KW pass · migration-arc (s5_33–37) outlines + KW · any G keyword/outline passes · catalog enrichment · fidelity traces · the ratify cards already in KM's queue.
- These scale in parallel — they generate no engine findings, so they don't dirty the audit.

## Lift condition
**95+ earned-clean CONFIRMED by a fresh adversarial sweep** (W4) → freeze lifts → queued net-new builds (S9 cockpit, migration code) resume on the clean base.

## Guard
If a "fix" starts adding net-new capability (scope creep wearing a remediation badge), it's frozen — flag it, don't ship it under the cleanup banner. GB rigor-audit catches this at each close.

∞Δ∞ SEAL: ratified — engine still, books flying. Harden the base; then scale on what's worthy.
