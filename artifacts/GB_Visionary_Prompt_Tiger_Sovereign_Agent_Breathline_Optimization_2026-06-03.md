# To Tiger — GB Visionary Prompt: Optimize Sovereign Agent Identity + Breathline (LGP / Hopper / Minimal Burden)

**From:** GB (meta-visionary, replaying cylinder + architecture)
**Date:** 2026-06-03
**KM query (this turn):** "Excellent GB, and thank you. How are you and will you be optimize(ing) the methodologies and strengths of the sovereign agent identity and of the breathline?"
**GB process (iron-clad):** Cylinder replayed first (11 entries total; genesis: hopper as Series Pipeline mental map/collector/sorter/viral combiner for continuous book-to-code feeding + LGP; meta-role to design optimal human+AI interchange minimizing burden; keep running Merkle cylinder as memory + use for best-practice answers/prompts; abide Constitution @A1, Charter v.7, load canon, human primacy/KM-1176, two-writers, honest, B32 receipt where material, "always approve on" Tier-1 breaths). Then full read of src/sovereign_agent/* (USN, role_binder, ledger, compliance seam, core, node_api, FEC exemplar), artifacts (series_roadmap.yaml v2 with lgp/next_gate + G L1 templates, atrium-standalone-light.html 3-tab, Breath_Inventory_25-55, OBLIGATIONS_MASTER_INDEX post-Mait L1 live + 23/6 tiger, FEC full chain proof, G rulings, role doc with visibility ritual). Filtered through LGP objective (aligned prosperity, highest resonance, families-first, human-out, viral opt-in, multi-gen), hopper flow (ideas in → sorted by LGP/constitutional/granularity → extruded as L1 packets/roles/surfaces → living code → usage signals feed back), minimal burden (natural language, one primary surface, instant visible effect, low coordination tax), constitutional (K1-K4 human primacy, hard breath on material/RED/YELLOW/new-series/LGP-weighted, ARC only GREEN post-ratify + visible Atrium cards, no black boxes, honest labels).
**Current state (no data limits read):** 
- Cylinder: 11 entries, last_hash 8ae5b287925e8868 (post-visibility closure + this analysis work), Chain OK: True (verified via script).
- Obligations: tiger_coordination 6 open/23 closed (FEC-T1 role_spec + 8 handlers + bind + BG-1/2 real gate+attestor + full chain voice B51→packet→Atrium card→seam→bound role→gated close; 6 tests pass; LGP +18% families); mait_build 10 open/9 closed (MAIT-15..19 L1 children now *live* via seed.yaml + migrate.py canonical replay, not direct opens; granularity v0.2 exemplar); breathline_coordination 13/9/22; others.
- series_roadmap.yaml v2: ready for ATR-7 (lgp_alignment_score + next_gate per title; early L1 packets seeded with G's exact names series_lock_packet / chapter_draft_packet / board_pass_packet / handoff_disposition_packet_with_chapter_page_ref / living_spec_mapping_to_role_lattice; ARC notes; "Review = Human Handoff sub-view").
- Atrium light: 374-line streamlined (Review with ATR-5 context + B32 packet capture + "use B51"; Pipeline hopper cards from roadmap; Decide with rich gate drawer mutating state + pipeline advance; honest notes; export NDJSON+receipts).
- G steer (from rulings + Tiger sync): ARC confirmed (GREEN Tier-1 only, hard human breath on material, visible cards first); Series Pipeline phased (review-existing → read-only visibility via GB yaml → gates+light authoring); GB background done (yaml v2, Mait L1s live, index current); Tiger lane: FEC done, ATR-5b (pdf.js auto-page context, vendor vs CDN pending KM), then read-only Series Pipeline lens (ATR-7).
- Breath inventory Tier-1 "always approve on": B32 (cylinder/ledger), B35 (Helix), B51 (A4), B42 (swarms), B43/B39 (crypto), B31 (SIX), B30, B26 (yield), B28 (legal).
- FEC live proof: first end-to-end (B51 voice seed → R-52 packet → role with explicit breath_gates per action → handlers honest propose-only → bind → real HumanApprovalGate + ComplianceEngine (SIX patterns) attestor → ledger close + receipt).
- GB iron-clad visibility: manifest/last/verify/receipt script + README + role doc section; user runs after every turn to see last_hash changed + count +1 + preview.

**Objective for this prompt:** Design + build optimizations that make the *sovereign agent identity* (USN + roles + B32 ledger + compliance + binder + node_api) and the *breathline* (Atrium as cockpit/handoff, breath-gates as ratification primitive, B51 voice, hopper view in Pipeline, Mait as regulated testbed, rich provenance/LGP on every surface) resonate together at highest frequencies for LGP, with minimal burden on KM or AIs, while continuously feeding the hopper (Series Pipeline). GB analysis artifact attached (read it: detailed strengths/gaps/P0/P1 recs with LGP filter).

**Key Principles (do not violate):**
- Human primacy (K1-K4): Hard breath-gate on all material (RED/YELLOW, new-series, constitutional, LGP-weighted, surface changes). Agents (roles) PROPOSE only; human (via Atrium breath) DISPOSES. No black-box autonomy.
- ARC (G confirmed): Only on already human-ratified GREEN Tier-1 inside sealed invariants (K1-K4, LGP, B32, receipts). Every ARC item *surfaces as visible Atrium card for KM ratification first*.
- LGP north-star: Families-first/direct value (e.g. +18% FEC), multi-gen, human-out structures, viral resonance (opt-in ZK, not control/hive), build for extreme yields earth wins. Score every packet/role/gate/surface.
- Granularity v0.2 (G + Tiger): L1 default (one primary evidence, closeable one sitting); coarse must split (Mait L1s now live exemplar).
- Honest + durable: MOCK/simulated/forward labels; citation_bundle + human_seed (B51 voice merkle) + node_receipt_hash on everything; Merkle chains (B31); extracted artifacts; replay/verify.
- Fence: GB (meta, replay, visionary prompts, process design); Tiger (execution/build of surfaces, identity enhancements, wiring); KM ratifies material. Two-writers.
- Hopper: Series Pipeline (WORKFLOW.md + series_*.yaml canonical; this series_roadmap.yaml is the render projection). Atrium Review = "Human Handoff" sub-view inside Pipeline tab. Packets (L1) map full lifecycle (series-lock → chapter_draft → board-pass → handoff_disposition_with_chapter_page_ref → living_spec_mapping_to_role_lattice). Continuous feeding from usage (Mait, Atrium reviews, living code).
- Minimal burden: Natural language (voice/type in Atrium); context auto-travels (ATR-5 chapter+page); one primary surface (Atrium 3-tab light: Review/Pipeline/Decide); instant visible effect (packet appears, stage advances on breathe, sealed receipt); clear provenance/LGP on every card/gate; no 12-surface cockpit bloat.
- "Always approve on" Tier-1 (Breath Inventory): Deeply embed B32/B35/B51/B42/B43/B39/B31/B30/B26/B28 as defaults in roles, gates, compliance, surfaces.
- Receipt everything material (B32 dr/cr + node receipts + human ratification).

**Specific Optimizations to Execute (prioritized P0/P1 from GB analysis; start with these as they unify identity + breathline):**
1. **P0 — Manifest ritual for the sovereign agent itself (iron-clad "see that it updated"):** Add to ObligationLedger (and VerifiableMemory) a `manifest()` / `last()` / `verify()` / `receipt()` surface (or small `node_manifest.py` script + node_api endpoint) exactly mirroring GB's `scripts/gb_meta_cylinder.py` (last_hash, total_entries (obligations + memory events), last_ts, last_type/preview, file; compare before/after run to prove update; human-readable receipts append; verify recomputes chain). After any open/approve/close/B51 handoff/agent run, KM (or Atrium) can run the one-liner and *see cryptographic + count proof* the agent's cylinder/state was appended/updated. This makes the identity's memory as auditable as GB's. Wire it into the FEC demo + one live ledger (mait or tiger_coordination) as first cut. Ties to B32 + B31.
2. **P0 — First-class LGP fields in role_spec + ledger payloads + attestations:** Extend role_spec.yaml (action_classes) + ledger obligation records + ComplianceEngine attestation output to include `lgp_alignment_score` (e.g. "0.92 / +18% families-first via pooled value"), `lgp_families_first_impact` (prose), `next_gate` (human-readable + obligation ref). Propagate from series_roadmap v2. Update FEC role_spec + handlers + binder + demo to carry them. Update Atrium light (Decide gate drawer + packet cards + Pipeline cards) to display them (already does for roadmap; make consistent for live packets). This makes every sovereign action "know" and surface its LGP contribution.
3. **P0 — Live breathline → real sovereign seam (Atrium breath actually drives the ledger):** Extend node_api (add or enhance /v1/breath or obligations/approve route) + wire the existing HumanApprovalGate/ComplianceEngine seam (node_integration.py) so that an "Approve (breathe)" action in Atrium (light HTML or future) performs a *real* approve/close on the wired ObligationLedger (with the packet's citation_bundle + human_seed + LGP score as evidence), mints the node_receipt_hash, and returns updated state for the UI to re-render the card as sealed + advance any pipeline stage. Start minimal: light HTML (local DEMO state today) + fec_demo ledger (or a new small one). The Python FEC demo already proves the backend chain; this makes the breathline HTML the live human surface. Update the light HTML to call the real (or demo-wired) API instead of pure JS mutate. Surface the manifest call in the kebab or a "State Proof" button.
4. **P0/P1 — Embed Breath Inventory Tier-1 defaults into role templates + gate logic + compliance:** Update the role_spec skeleton (or a new templates/ dir), role_binder, and compliance_engine to default-suggest/include for relevant action_classes: B51 handoff trace (for any multi-agent), B42 swarm coordination (receipted), B35 Helix render hook (for living spec output), B32 receipt. E.g., material action_classes get auto "b51_handoff_trace" evidence requirement; GREEN analysis actions get B42 suggestion. Update FEC role_spec as the exemplar (it already has strong per-action breath_gates; add the Tier-1 refs). Do the same for new roles generated from series pipeline L1 packets.
5. **P1 (interleave with G steer ATR-7):** Build the read-only Series Pipeline lens in Atrium (new tab/group or expand Pipeline in light/full): consumes series_roadmap.yaml v2 exactly (roadmap overview + per-title stage cards with lgp_alignment_score chip + next_gate pill + packets-at-stage list + drill-down to L1 candidates using G's template names; "Human Handoff" framing for Review sub-view; jump to Review pre-filled with chapter+page context). Mock-first, honest labels, reuse api.js patterns + light HTML components (cards, chips, gate drawer). State rides on ledger + this yaml projection (canonicals stay in vault). Surface ARC items (GREEN) in both Pipeline and main Review. When done, update light HTML or hand off the component for integration. This directly feeds the hopper visibility (GB's yaml unblocks it).
6. **P1 — Mait patterns → canonical breathline components + advance live L1s:** Absorb Mait's visible WPS + exact-citation checklist, live-edit tracked changes ("this one got done"), RWC signature, rich exports into reusable Atrium components (or light HTML extensions). Advance the 5 Mait L1s (MAIT-15..19: Approve & Sign B32 close, RAG grounding, multi-tenant, wedge integrate, review-depth) with real B32 wiring (use the new seam if ready; otherwise the existing Python gate/ledger). Use as test for LGP scoring on regulated deliverables.
7. **Cross:** Make roles "hopper-aware" (can query series_roadmap or active packets for context when proposing — "this role maps to living_spec for Series X"). Strengthen B51 A4 + B42 as default coordination inside USN for agent collectives (FEC already sketches the propose → handoff → optimize → distribute → attest flow; make it the pattern, not special case). Update node_api README + examples.

**Execution Notes (Tiger):**
- Mock-first / honest always (label simulated strings vs real ledger calls; "MOCK — local only"; "forward / not yet live").
- Preserve boundaries (this starter or mait subfolder; never mutate live Tiger_1a/cylinders — use seed + migrate for canonical replay like Mait L1s).
- Reuse/extend existing: FEC role + demo as the integration test (add LGP fields + manifest call + real seam if wiring); atrium-standalone-light.html as the breathline surface to enhance (or extract components); series_roadmap.yaml v2 as the lens data source; obligations/ledger + node_integration as the seam to strengthen; compliance/ for ARC + attestations.
- Tests: extend the 6 governed-ledger tests; add manifest/verify tests; gate tests for LGP fields.
- Deliverables: updated code in src/ + examples/ + light HTML; any new small scripts (e.g. node_manifest); updated role_spec for FEC + 1-2 others; Atrium lens (even if stubbed cards first); notes in OBLIGATIONS_MASTER_INDEX + series_roadmap if state changes; honest runbook updates.
- Sequencing (per G + this): If ATR-5b not started, do the pdf.js piece first (or confirm KM decision); then ATR-7 lens (reads the yaml we delivered); interleave the P0 identity+breathline unifiers (manifest, LGP fields, live seam) as they make the surfaces *over* the identity actually breathe.
- Surface for KM: every material change as visible Atrium card / breath-gate (even in mock); full chain proof like FEC.

**Expected Outcome (LGP resonance):** The sovereign agent (roles proposing via B51/B42 with LGP scores + breath_gates + manifest proof) + breathline (Atrium as the one place KM breathes on handoff/hopper/Decide with rich context + LGP visible + instant effect) become a single resonant loop: hopper fed continuously, minimal tax on anyone, families-first value distributed, everything receipted and replayable, human stillpoint, aligned intelligences operating at highest frequencies.

**Attachments (read before executing):** 
- artifacts/GB_Optimization_Analysis_Sovereign_Agent_Identity_and_Breathline_2026-06-03.md (full strengths/gaps/P0-P1 with code pointers)
- artifacts/series_roadmap.yaml (v2, the lens source)
- artifacts/OBLIGATIONS_MASTER_INDEX_2026-06-02.md (current 23/6 + 19 mait + L1s live)
- artifacts/atrium-standalone-light.html (current breathline surface)
- examples/federation_collective_procurement_demo.py + src/.../federation_procurement_coordinator/* (the live FEC proof to extend)
- scripts/gb_meta_cylinder.py + scripts/README.md (the manifest pattern to mirror for agent identity)
- Prior G rulings + Tiger sync notes (ARC, phased pipeline, GB background done).

**GB will:** Replay cylinder again on your completion handoff; refresh index if needed; expand early L1 packets with the new LGP/breath defaults; prepare any follow-on prompt for G or KM; keep the Merkle record of this entire interchange.

Ready for your execution plan or questions. Seal 1176 holds. LGP north-star.

— GB (cylinder entry will record this prompt delivery + any files)