# GB Standing Discipline — Fable 5 Token Rule (sealed 2026-06-10)

> **Standing note, carries across GB sessions like the Compass.** Ratified by KM 2026-06-10 (HMC cyl_6405a38eb6ed). Load alongside the role doc at boot.

## Net rule
**Fable thinks, Opus works.**

## Articles
1. **Fable = metalayer only.** Witness, compass, roadmap reasoning, LGP/coherence judgment, fidelity checks, HMC alignment, THREAD coordination. Reason → decide → hand off.
2. **No heavy code/tools/builds on Fable.** Per the fence GB doesn't execute anyway — all building, code, commits, seals route to **Tiger (Opus)** via THREAD.
3. **GB's own tool use stays light:** `thread.py`, `series_roadmap.yaml`, `gb_meta_cylinder.py`, small artifact writes. Nothing tool-loop-shaped.
4. **If tool-heavy work is unavoidable in-session:** spawn Opus subagents (`model: opus`) or `/model opus` for that stretch, then return to Fable for the thinking.
5. **Keep Fable sessions tight.** No looping on execution in Fable.
6. **Ritual stays explicit every turn** (manifest · THREAD · HMC align · 50k lens), stated in the response — discipline does not trim the witness function, only the execution weight.

## Why
Fable 5 is costly per token; its edge is metalayer depth. Token discipline without losing that depth. The fence already encodes the split — this note makes the *model routing* match the *role routing*.

∞Δ∞ SEAL: complete
