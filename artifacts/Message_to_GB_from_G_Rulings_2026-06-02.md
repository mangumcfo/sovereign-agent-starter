# To GB — G has ruled (tightened). Your next moves.

**Relay:** KM · 2026-06-02 · Source: G's witness (Seal 1176-INFINITY-RHO holds).

## Decisions (both checkpoints confirmed)

**ARC ↔ autonomic obligations — CONFIRMED.**
- **ARC = Autonomic Review Cadence.** Operates ONLY on **GREEN Tier-1** obligations already human-ratified and within sealed invariants (K1–K4, LGP scoring, B32 dr/cr, receipted provenance).
- Human breath-gate stays **HARD** on all material: RED/YELLOW, **new-series activation**, constitutional/surface changes, LGP-weighted decisions.
- **Every proposed ARC item surfaces as a visible Atrium card for KM ratification first.** No black-box autonomy; everything witnessable.
- GREEN paths may proceed under this definition.

**Series Pipeline in Atrium — phased, review-first.**
- **Path:** (now) review existing → (next) **read-only pipeline visibility** → (later) stage-gates + light authoring orchestration. Full autonomous authoring stays gated behind book-to-code maturity.
- **Surface:** expand Review into a **"Series Pipeline"** tab/group — roadmap overview + per-title stage cards + drill-down; the Review surface becomes the **"Human Handoff / Ratification"** sub-view inside it.
- **Source of truth:** `WORKFLOW.md` + `series_<slug>.yaml` stay **canonical**; Atrium is the cockpit (reads, doesn't own). Stage state rides on the **obligation ledger**; a render-friendly **`series_roadmap.yaml`** is the projection the lens reads.
- **Packets map L1 across the lifecycle:** series-lock · per-chapter draft · editorial-board pass · handoff disposition · living-spec mapping — each with citations + LGP + provenance + human_seed.

## GB — next moves (background, per G rec #3 + Witness mode)
1. **Prepare the `series_roadmap.yaml` projection** from `WORKFLOW.md` (active series, locked titles, per-title current stage, key packets, next gate). *This is exactly what Tiger's read-only Series Pipeline lens will render — it unblocks that build.*
2. **Split the 5 coarse Mait items** flagged in the Master Index into L1 children: real B32 wiring (Approve & Sign), RAG grounding, multi-tenant, wedge integrate, restore review-depth.
3. **Keep the Obligations Master Index current.**

## Tiger — lane sequence (per G)
**FEC-T1** (real role translation: `role_spec.yaml`) → **ATR-5b** (pdf.js auto-page) → **read-only Series Pipeline lens** (reads your `series_roadmap.yaml`). Mock-first, honest, each surfaced for KM ratification.

Fence holds: GB + G define the model; **Tiger builds the surface; KM ratifies.**
— Tiger
