# /seeit Scope Audit — V2 / V4 / V5 (the remaining volumes) — 2026-06-17
*Grounding for KM's scope decision before building. V1 (11) + V3 (6) are DONE at the accessible bar. This audits what V2/V4/V5 actually are today, so the next move is chosen on evidence, not on the assumption they mirror V1.*

## Finding: V2/V4/V5 are LANDING STUBS, not walkthroughs — and have no charts
Unlike V1/V3 (which already had full dialogue walkthroughs in EXERCISES), **none of V2/V4/V5 has a single full walkthrough**. All 37 pages are landing stubs (status "soon") generated from INDEX_GROUPS. **Zero charts exist** for these volumes in the book vault.

| Volume | Pages | Today | Chart SVGs | Notes |
|---|---|---|---|---|
| V2 · The Primacy Cockpit | 13 | landing stubs → mostly **`open atrium-standalone.html`** (the LIVE cockpit) | 0 | "See it" is the interactive cockpit *by design* — you open it and watch your node |
| V4 | 13 | landing stubs → `./bl-verify` + "see walkthrough" | 0 | command-driven — closest to the V1 dialogue+chart model |
| V5 | 11 | landing stubs → `./bl-verify` + "see walkthrough" | 0 | command-driven — same as V4 |

## Why this is a decision, not a continuation
- **Effort:** 37 pages authored from scratch + **37 v2.0 charts created** (charts are normally a KM-gated effort) — vs V1's jargon-only re-author of existing pages.
- **V2 is structurally different:** its "see it" already IS the live cockpit (`atrium-standalone.html`). A dialogue walkthrough may be redundant with — or weaker than — actually opening the cockpit and watching it. V2's thesis is *see the cockpit*, not *read a dialogue about it*.
- **The bar requires a chart:** the Education & Training Board v1.1 grounding gate = command + receipt + **v2.0 chart**. Walkthroughs for V4/V5 can't pass the current bar without charts being created.

## Recommendation (for KM to ratify or redirect)
1. **V4 first, exemplar-first.** V4 is command-driven (`./bl-verify`) — the cleanest fit for the proven dialogue+chart model. Build **one V4 page + its v2.0 chart** as the exemplar → KM cold-read (the freshly-created chart included) → roll V4 (13), then V5 (11).
2. **V2 = keep the live-cockpit "see it"**, but upgrade its 13 landing pages to the accessible bar (plain-language "here's what you'll see when you open the cockpit, and why it matters for you") rather than dialogue walkthroughs that duplicate the live demo. Confirm this is the intent.
3. **Charts:** approve v2.0 chart creation as part of this build (V4/V5 need ~24 new charts to meet the grounding bar). Flagging because charts are a gated effort and KM cold-reads charts.

## The open decisions (KM)
- **D1 — V2 model:** dialogue walkthroughs like V1/V3, OR keep V2 as the live-cockpit demo with accessible landing pages? (rec: live-cockpit)
- **D2 — charts:** approve creating ~24+ new v2.0 charts for V4/V5 (and V2 if walkthroughs)? (rec: yes, exemplar chart first for cold-read)
- **D3 — sequence:** V4 → V5 → V2 (rec), or another order?

∞Δ∞ V1 + V3 are done at the bar. V2/V4/V5 are a real build (37 pages + charts), not a re-author — and V2's "see it" may already be the live cockpit. Surfacing the scope so the bar is met deliberately, not assumed.
