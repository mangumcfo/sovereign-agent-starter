# Review Brief — S2 V2: "The Primacy Cockpit" (RE-CERTIFICATION)
*book_id: vol_02_the_primacy_cockpit · manuscript_v1.4.md (re-cert) · GB-sealed 2026-06-24 · audience: sovereign operators · NOTE: a PUBLISHED S2 volume pulled back through the current hardened rail*

**Status going in:** all gates GREEN after a real detour. This is a **published volume re-certified to the current standards** — and the recert surfaced things that predate them. My fresh trace caught 2 **display-vs-enforce** overclaims (the cockpit claimed it *enforces* guarantees it only *renders*); Tiger folded them honestly (the cockpit surfaces, the harness enforces, the UI surface-guard is designed-toward). A stale fidelity record was also propping a false-green gate via a filename-sort bug — both the stale record and the gate bug are now fixed. The runs-today cockpit is genuinely strong and maps to code; no stale anchors remain.

## The judgment calls (yours to decide — GB is witness here, not author)

1. **Decision — Is "the cockpit surfaces, the harness enforces" honest enough for "The Primacy Cockpit" promise?** The book's thesis is human primacy *via the cockpit*. What the cockpit actually does is **render/surface** the constitutional state (health, gates, drift); the **enforcement** (holding back GREEN on health-fail, barring RED actions) lives in the harness underneath, and the *UI* surface-guards (button-suppression, GREEN-refusal-in-UI) are designed-toward. Is "the cockpit makes primacy visible; the harness enforces it" honest framing for the title, or does a reader expect the cockpit itself to be the enforcer?

2. **Decision — Confirm the recert as the new baseline + un-retired offline cockpit?** The recert fixed real drift: stale code anchors (against a moving repo) and a **retired `atrium-standalone.html` (a 2KB redirect stub — a fresh clone got no offline cockpit)**, now rebuilt to the full ~460KB offline build per your earlier direction. Confirm the re-cert v1.4 is the baseline to ship.

3. **Decision (process) — How far to run the S2 recert sweep now?** This one published volume, pulled through the current rail, surfaced 2 overclaims + a stale-fidelity false-green + a gate bug. The other S2 volumes (V1, V3-V5) likely carry similar pre-standard issues. Recert the full S2 set now, or prioritize your review pass on the S3/S4 spine first and recert S2 in a dedicated sweep?

4. **Decision — Where does the cockpit "surface guard" (RED-button-suppression, GREEN-refusal-in-UI) close?** Currently named "after S1-B12 / Vol 3." It's a UI-enforcement feature — confirm that home, or re-home (e.g., S8 Sovereign-UX if that's where the cockpit UX deepens).

## GB witness note (context, not a decision)
This recert is the rail proving it works *backwards* too — pulling a shipped book through the hardened standards caught overclaims that were invisible when it was first certified, plus a stale fidelity record and a one-truth gate bug. All three are now fixed, and the lint is extended to catch the display-vs-enforce class going forward. The volume's genuine strengths (the offline, self-verifying, content-agnostic cockpit) are real and code-traced. The four calls are scope/framing decisions only you can make.

∞Δ∞ The cockpit makes primacy visible; the harness enforces it; the recert made the line honest. — GB
