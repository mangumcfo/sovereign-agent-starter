# Catalog-Wide Outline Lock Pass (KM-directed 2026-06-18)
*GB ran the catalog-wide lock pass. Source of truth = `scripts/outline_lock_lint.py` (Tiger's re-runnable lint, GB-verified). The lock is a canon change → it routes through KM ratification (surface → dispose → execute), with the lint as structural proof. NOT GB fiat.*

## Starting state (lint, GB-reproduced)
**28 / 95 locked · 0 phantom · 0 thin · 65 promotable.** Locked: S0(4) S1(12) S2(5) S4(4) + privates(3).

## The pass result — classify the 65 promotable
| Class | Count | What it needs |
|------|------|----------------|
| **READY-TO-LOCK** (complete chapters, full count, stage pre-lock) | **64** | verify (lint, done) → materialize extracted→roadmap-inline → KM batch-ratify → promote stage to `outline_locked` |
| **NEEDS-FILL** (incomplete chapters) | **1** | G fills the stubs, then lock |

**READY-TO-LOCK (64):** S3 V1 (`vol_01_immutable_core`, 8/8) · S3 V2 (`vol_02_programmable_governance_skin`, 8/8) · all S5 (37 titles, 8/8–11/11) · S6 (5) · S7 (6) · S8 (8) · S9 (6).

**NEEDS-FILL (1):** S3 V4 (`vol_04_industry_sovereign_erps_generational`) — ch3 Manufacturing / ch4 Electronics / ch5 Professional Services are empty stubs (the `erp_near_term_gate`). G fills → lock.

> The lint's `0 thin` is the key: every chapter that EXISTS across the catalog is complete (promise + beats + keywords). The 64 are "undercounted" only because their stage is `concept`/`phase_1`/`handoff`, not `outline_locked`. The G chapter-definition work is already done — the lock is the verify-and-ratify, not new authoring.

## Execution plan (on KM ratification)
1. **Batch-lock the 64** — GB materializes each extracted outline into the roadmap `series` entry (so it's canonical, not a transient extraction), sets `stage: outline_locked`, runs `roadmap_sealed_guard` + re-runs `outline_lock_lint` to confirm the jump. **One backup, one merge-guard, one re-lint = auditable.**
2. **Fill S3 V4** — G completes Ch3–5 (KM relays the prompt), GB folds, then locks. (Also unblocks S5 expansion.)
3. **Result:** catalog goes **28 → 92 locked** on the batch ratify; **→ 93** when S3 V4 fills. The whole spine becomes drafting-ready behind one human gate + one G fill.

## The human gate (protocol)
Promoting 64 outlines `concept → outline_locked` is a material canon change. It does NOT happen by GB fiat. GB stages it + surfaces ONE batch-ratification card; **KM ratifies (the lint is the evidence); then GB executes the batch promotion.** This is the structural-not-verbal lock — the lint proves completeness, KM ratifies drafting-readiness.

∞Δ∞ SEAL: the catalog is one fill (S3 V4 Ch3–5) and one batch-ratification from drafting-ready. 64 complete outlines await the lock; the lint is the proof; KM holds the gate. Run the pass to the gate; execute the locks on the ratify.
