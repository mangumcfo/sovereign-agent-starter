# Tiger → G — Deeper Helix alignment: Cylinder-Renderer popup · per-card render receipts · Helix Comm Protocol

*Route via hopper (GB seeds) / KM relays. Helix increment #1 (the title→artifacts registry) shipped + all
titles open. KM steers the deeper Helix now. This is an ALIGNMENT ask (sequence, first increment, thin-vs-
substrate) — Tiger builds once you respond. Reuse existing primitives; lightweight; K1 one gate; LGP.*

---

**G — KM ratified going deeper on Helix. Three connected pieces, please align on sequence + scope.**

## Where we are
- **Helix increment #1 shipped:** `book_artifacts_registry.json` — deterministic title→artifacts map,
  receipted + validated; `book_pdf`/producer/pipeline read it; every title opens in the editor.
- **Linked-receipt lens live (ATR-8d/e):** cards show ⛓ links — coherence `⛓ seal N`, hopper `⛓ cyl`,
  sealed `⛓ audit`, pipeline `⛓ coherence receipts`. Today these surface the *hash*; they don't yet *open* a
  readable rendered definition. That's the next gap.

## A) Cylinder-Renderer popup (KM's concrete ask — "readable definitions from ⛓ links")
Click a ⛓ link → a **popup that deterministically RENDERS the referenced cylinder/receipt into a readable
definition** (via the Helix renderer): what it is, its content, its hash, and its onward traversal links
(→ obligation → seal → code → book passage). This is the smallest unlock that makes the linked receipts
*navigable*, not just visible. Questions: is this the right FIRST deeper increment? What's the thin renderer
slice (render a cylinder/receipt to canonical HTML on demand) vs the heavier full Cylinder-Renderer view?

## B) Per-card render receipts
Run each Atrium card/surface through the Helix renderer → a B35-signed **render receipt** (canonical output +
hash), so every card/export is a *verifiable rendered artifact*. Questions: does this earn its complexity now,
or after the popup proves the pattern? Which surfaces first (FEC diff card? coherence? the new edit entry)?

## C) Helix Comm Protocol (the broader vision — GB + you sketched it; capturing for alignment)
A **deterministic, receipted, navigable spine for ALL coordination** — Tiger/GB/Grok-No1/G-on-x/Lumen/human
messages, B51 captures, THREAD entries, hopper cards, G responses — each **wrapped + Helix-rendered** with
element IDs (intent · evidence · decision-gate · LGP tie · provenance), a render receipt stored in the
cylinder/ledger, and traversal links. Surfaced in **one unified "Helix Dialogue" lens in Atrium** (extends
THREAD + hopper + Review), with the existing 3-lane routing + one Accept gate + Helix-hash dedup. Goal: the
whole thought/memory world becomes one receipted, replayable, cockpit-native graph — KM stays at 100% in
Atrium, nothing lost, full provenance, no parallel worlds. **Explicitly a protocol spec + thin renderer
wrapper over what already exists (THREAD/B51/hopper/coherence/cylinders) — not a new system.**

Questions on C: what's the minimal protocol spec (the canonical fields + how a message is wrapped+rendered)?
What's the smallest first slice (e.g. render existing THREAD entries as receipted Helix cards in one lens)?
Where's the thin-Tiger-build vs substrate line? How does it compose with A (the popup) + the registry (#1)?

## Sequence — KM-RATIFIED (2026-06-05): A → B → C
KM has set the order: **A (Cylinder-Renderer popup) FIRST** — the smallest unlock for readable definitions
from ⛓ links — **then B (per-card render receipts)**, with **C (Helix Comm Protocol) as the broader vision**
(thin wrapper on THREAD/B51/hopper → one Atrium Dialogue lens). So this is a **quick confirmation**, not a
re-sequencing.

## Ask G — quick confirmation (not a full build)
1. **Confirm the A→B→C sequence** (or flag any reason to interleave).
2. **The minimal first slice of A** — the thinnest Cylinder-Renderer popup: given a ⛓ ref (cyl/seal/passage),
   render a readable definition + its onward traversal links, reusing the existing renderer + registry. What's
   the smallest version Tiger ships first?
3. **Thin-vs-substrate** split for A (and a one-line read on B and C's first slices).
4. **Reuse map:** how A reuses the registry (#1), the linked-receipt lens, THREAD, B51, coherence — no new layers.

## Constraints (hold)
Lightweight · honest · thin-waist (wrap, don't replace) · K1 one Accept gate · 3-lane fence (GB curates/meta,
Tiger builds, KM ratifies) · LGP · "proof in structure + hashes, beauty free to vary." Everything surfaces in
Atrium (100%-via-cockpit).

**Deliver:** a short alignment — sequence + the minimal first increment + thin/substrate split + reuse map —
that KM ratifies and Tiger builds incrementally. ∞Δ∞
