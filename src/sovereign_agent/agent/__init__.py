"""sovereign_agent.agent — the node's local mind (loopback model only), as a package module so both the CLI
(scripts/node_agent.py) and the node_api chat route can import it (package→package; never package→scripts)."""
